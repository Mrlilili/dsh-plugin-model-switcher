/**
 * DSH Plugin: Model Switcher — Host (no-op)
 *
 * All logic lives in the Client half. The Host half exists only to satisfy
 * the bundle format requirement.
 *
 * @module dsh-plugin-model-switcher
 */

export function apply() {}